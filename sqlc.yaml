version: "2"
sql:
  - engine: "postgresql"
    queries: "repository/query"
    schema: "repository/migrations"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
